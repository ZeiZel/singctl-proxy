apiVersion: v1
kind: Service
metadata:
  name: {{ include "singctl-license.fullname" . }}
  labels:
    {{- include "singctl-license.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "singctl-license.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
