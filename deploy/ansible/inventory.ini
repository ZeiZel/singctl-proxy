# Static inventory (optional — bootstrap.sh passes the host inline with `-i HOST,`).
# After bootstrap, you can target the box by its SSH alias:
#
#   ansible-playbook -i inventory.ini playbook.yml
#
[license]
remote-singctl-server
