/bin
/dist
/.DS_store


# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# macOS Finder metadata
.DS_Store
**/.DS_Store

# Compose deployment secrets (private key, admin token, webhook secret)
deploy/compose/.env

# macOS netextension build artifacts (xcodegen output + Xcode DerivedData)
/packaging/macos/netextension/build/
/packaging/macos/netextension/SingctlProxy.xcodeproj/

# macOS Singctl app build artifacts (xcodegen output + Xcode DerivedData)
/macos/Singctl/build/
/macos/Singctl/build_check/
/macos/Singctl/Singctl.xcodeproj/
/singctl
/server
